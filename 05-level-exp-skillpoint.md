# 05. 경험치 · 레벨 · 스킬포인트

## 작업 내용

- **경험치 획득**: 몬스터 처치(슬라임 8, 화난 버섯 14), 퀘스트 완료(화난 버섯 소탕 80). 처치 시 `+8 EXP` 표시
- **레벨업** (Core `LevelRules`)
  - 요구 경험치 `40 × L^1.5` (Lv1→2: 40, Lv5: 447, Lv10: 1,265), 여러 레벨 한 번에 오름, 넘친 경험치 이월, 최고 레벨 250
  - 레벨당 능력치: HP +12, 공격·마법 공격 +2, 방어·마법 방어 +1, 레벨업 시 체력 회복
  - 레벨업마다 **스킬포인트 1** ("LEVEL UP!" + 알림)
- **스킬 레벨**: 스킬창(K)의 `+` 버튼으로 포인트를 써서 올림, **최대 10**, 레벨당 위력 +10% → 실제 데미지 반영 (`SkillRules.PowerAt`)
- **UI**: 화면 맨 위 가로 전체 경험치 띠(레벨·경험치·진행률%), 상태창 이름 옆 `Lv.N`, 스킬 버튼에 남은 포인트 `+N`, 스킬창·툴팁에 스킬 레벨과 현재/다음 레벨 위력
- 성장 상태는 `PlayerProfile.Progression`(`PlayerProgression`) — 능력치 = 레벨 1 맨몸 + 레벨 성장 + 장비
- 스킬 레벨 0 = 아직 배우지 않음 (나중에 만들 스킬마스터 NPC 준비), 지금은 새 게임에서 모든 스킬 레벨 1
- **세이브 v4**: level / exp / skillPoints / skills. 예전 세이브는 레벨 1 + 쓰던 스킬 모두 레벨 1

## 스크린샷

![](images/05/progress-skills.png)

![](images/05/progress-hud.png)

## 문제와 해결

| 문제 | 원인 / 해결 |
|---|---|
| 기획 변경: 스킬 트리를 정리하면서 레벨업 지급량 조정 | 레벨업당 스킬포인트 3 → 1 (`LevelRules.SkillPointsPerLevel`) |
| 맨 위에 띠를 넣을 자리가 없음 | 상태창·몬스터 정보 박스·도움말·알림을 약 8px씩 내려 띠(높이 18) 자리를 만듦 |
| 띠 글자가 작고 노란 바탕 위 흰 글자 대비가 약함 | 캡처를 확대해 확인 → 글자 12 → 13px, 레벨 굵게, 채움색을 조금 진하게 |
| 불러온 세이브 값이 깨져 있을 수 있음 (음수 레벨, 요구치보다 큰 경험치, 삭제된 스킬) | `PlayerProgression.Load` 에서 보정하고 테스트로 확인 |
| 레벨이 바뀌면 최대 HP 가 달라져 HP 를 먼저 넣으면 잘림 | 불러오기 순서를 레벨 → 능력치 재계산 → 장비 → HP 로 정리 |

## 남은 일
- 경험치 요구치·성장 수치는 임시값 → 밸런스 조정 필요
- 스킬 습득 조건(레벨·직업·전직)은 스킬마스터 NPC · 직업별 스킬 트리 작업에서 이어서
