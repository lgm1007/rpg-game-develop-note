# 19. 스킬 재사용 대기시간 체계 개편

## 작업 내용

- 레벨이 높아질수록 길어지던 스킬 재사용 대기시간을 스킬 종류별 고정값으로 통일했다
  - 직업의 레벨 1 스킬(전사 강타 · 법사 에너지 볼): 1.5초
  - 다른 단일 대상 스킬 · 회복 · 순간이동: 2.5초
  - 범위 공격(대쉬 포함): 4초
  - 패시브: 재사용 대기시간 없음
- 쿨다운 결정 규칙을 Core `SkillRules.CooldownFor` 에 모으고, `GameDatabase.AddSkill` 에서 등록할 때 값을 지정한다. 기존 스킬마다 따로 적던 값은 제거했다
- 기본 공격 `attack.basic` 은 스킬이 아니므로 기존 공격 간격 0.9초를 유지한다. 공격 속도 패시브도 기본 공격 간격에만 적용된다
- 전사 시작 스킬 강타의 MP 소모를 3에서 2로 조정했다. 쿨다운이 4초에서 1.5초로 줄어도 쉬지 않고 사용할 때 MP 회복량이 소모량보다 많도록 맞췄다
- 스킬창·핫바 재사용 표시와 남은 시간 알림은 `SkillDefinition.Cooldown` 을 사용하므로 새 고정값을 따른다
- 세이브 필드는 변경하지 않았다
- **테스트**: Unity 6000.6.2f1 배치 모드 컴파일 + EditMode 테스트 194개 통과
- 커밋: `a352135` — `스킬 종류별 재사용 대기시간 고정 (#25)` (게임 저장소 `main`)

## 스크린샷

이번 작업은 쿨다운 규칙과 데이터 수치 변경이라 별도 게임 화면 캡처를 만들지 않았다.

## 문제와 해결

| 문제 | 원인 / 해결 |
|---|---|
| 레벨이 높은 스킬일수록 재사용 대기시간이 길어짐 | 스킬 데이터마다 다르게 지정하던 값을 제거하고 대상 방식 기준의 고정 규칙으로 통합했다 |
| 강타를 새 간격으로 계속 쓰면 전사 MP가 회복보다 빨리 줄어듦 | 1.5초 간격에서 MP 소모량이 초당 회복량보다 낮아지도록 강타 MP 소모를 2로 조정했다 |
| 기본 공격과 레벨 1 직업 스킬을 혼동할 수 있음 | `attack.basic` 은 별도 공격 간격을 유지하고, 강타·에너지 볼만 1.5초로 분류했다 |

## 남은 일

- Unity 에디터에서 스킬창·핫바의 쿨다운 덮개와 실제 재사용 가능 시점을 플레이로 확인
- 전사 시작 스킬 MP 소모량과 고정 쿨다운의 체감 밸런스 확인
